'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _support = require('@kernel-js/support');

/**
 *
 * @param  {string}        key
 * @return {object}
 */
var splitKey = function splitKey(key) {
  var keyParts = key.split('.');
  var root = keyParts[0];

  delete keyParts.splice(0, 1);

  return {
    root: root,
    nested: (0, _support.isEmpty)(keyParts) ? null : keyParts.join('.')
  };
};

exports.default = splitKey;
module.exports = exports['default'];